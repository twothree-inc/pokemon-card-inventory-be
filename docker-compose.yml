services:
  api:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: pokemon-be-api
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - ./src:/app/src
      - ./scripts:/app/scripts
    restart: unless-stopped
